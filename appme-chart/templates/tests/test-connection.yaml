apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appme-chart.fullname" . }}-test-connection"
  labels:
    {{- include "appme-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appme-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
