apiVersion: v1
kind: Service
metadata:
  name: {{ include "appme-chart.fullname" . }}
  labels:
    {{- include "appme-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appme-chart.selectorLabels" . | nindent 4 }}
